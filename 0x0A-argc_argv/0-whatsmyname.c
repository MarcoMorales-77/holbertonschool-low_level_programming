#include <stdio.h>

/**
 * main - Entry point
 * @argv: number of arguments
 * @argc: string with arguments
 * Return: 0.
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
